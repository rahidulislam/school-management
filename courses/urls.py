from django.urls import path
from courses import  views

app_name = 'courses'
urlpatterns = [
    # path('add_course/', views.AddCoruse.as_view(), name='add_course'),
    # path('manage_course/', views.ManageCourse.as_view(), name='manage_course'),
    # path('course_edit/<int:pk>/', views.CourseUpdate.as_view(), name='course_edit'),
    # path('course_detail/<int:pk>/', views.CourseDetail.as_view(), name='course_detail'),
    # path('course_delete/<int:pk>/', views.CourseDelete.as_view(), name='course_delete'),
    # path('add_subject/', views.AddSubject.as_view(), name='add_subject'),
    # path('subject_detail/<int:pk>/', views.SubjectDetail.as_view(), name='subject_detail'),
    # path('manage_subject/', views.ManageSubject.as_view(), name='manage_subject'),
    # path('subject_edit/<int:pk>/', views.SubjectUpdate.as_view(), name='subject_edit'),
    # path('subject_delete/<int:pk>/', views.SubjectDelete.as_view(), name='subject_delete'),
]